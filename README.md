# Item / Material price calc

This is a part of my project of alumium calcs. In this project, I wish to simplify the calculation of item prices using an automated fill and tabulate system.
The datalist is populated by **JSON** and then the user can select from the dropdown. if unavalaible, the user can manually add an item.